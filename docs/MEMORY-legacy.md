# MEMORY-legacy.md — Spilled from MEMORY.md "Discovered durable knowledge"
# Extracted historical/completed items unlikely to be revisited.

- **Windows console**: Requires `sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')` for emoji output (only relevant if CLI is re-added)
- **Feature roadmap**: P0 = 品牌线索库 + SKU分级治理（含卖家健康度）; P1 = 批量操作 + 智能提醒; P2 = 竞品监控 + 多数据源线索. REMOVED: Data merge (page 7), AI email generation in leads.py, standalone seller health page (page 5, merged into SKU分级治理) — user found them too complex.
- **Streamlit single-page white screen pattern**: In the `st.tabs()` single-page model, ALL 6 modules share one Python execution thread. Any unhandled exception during Streamlit's re-execution cycle (triggered by button clicks, widget updates, etc.) crashes the JavaScript client with a white screen — Python try/except boundaries are insufficient because Streamlit uses WebSockets and the JavaScript client is outside Python's control. The architectural fix is to split modules into independent pages via `pages/` directory.
- **Streamlit background process crash**: `run_in_background=true`启动的streamlit如果启动后崩溃，后台不会显示错误日志。必须前台启动（设timeout或不设run_in_background）才能捕获崩溃输出。排查启动问题时始终用前台模式。
- **SKU grading page current state** (2026-06-26): `pages/6_卖家分析.py` now ~1406 lines. Sections: Valorant CSS injection, seller overview (charts), category/brand mappings, SKU processing functions, seller health calc, SKU grading UI with upload/save/history review. History review section shows full analysis results (3 tabs + summary stats + charts) identical to first-upload view.
- **Redundant code audit (2026-06-29)**: `src/web/config.py` is pure re-export shim (can remove). `experience_library.py` `suggest_reply()` is dead CLI code. `emails.py` has unused `import traceback`. `np.log10` in SKU page can be replaced with `math.log10`. `fetch_website_content_js()` (playwright) confirmed used by `leads.py` line 39 — NOT dead code. `__pycache__` in 6 source dirs. `package.json`/`node_modules/` unused by Python project. User said "我来决策" — awaiting cleanup decisions.
- **Full cleanup completed (2026-06-29 + 2026-07-13)**: Deleted __pycache__ (6 dirs), package.json, package-lock.json, .idea/, node_modules/. Fixed dead code: removed LEADS_PATH, load_leads(), unused render_section_header import.
- **`src/web/config.py` is a pure re-export shim**: Just `from src.config.settings import *`. Could be removed if imports updated.
- **`experience_library.py` dual call pattern**: `suggest_reply()` (SDK client) for removed CLI mode; `suggest_reply_web()` (raw HTTP) for web. SDK method may be dead code.
- **Dead references**: `LEADS_PATH = data/leads.csv` in settings.py — file doesn't exist. `volcengine-python-sdk[ark]` in requirements.txt only used by dead CLI code path.
- **Streamlit sidebar name = filename**: Navigation names auto-generated from `pages/` filenames (strip numeric prefix + underscore). To change sidebar display, MUST rename the file.
